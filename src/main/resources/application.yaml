server:
  port: 8080
  servlet:
    context-path: /api/v1/zen8labs-system

spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/banking'
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true